version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.truly_intelligent
    container_name: genius-ai-truly-intelligent
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=INFO
    env_file:
      - ./backend/.env.intelligence
    restart: unless-stopped
