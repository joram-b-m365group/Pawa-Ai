version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.advanced
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=INFO
    volumes:
      - ./backend:/app
